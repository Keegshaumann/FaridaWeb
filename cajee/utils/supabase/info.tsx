/*
 * Supabase connection details for the front-end.
 *
 * Replace both values with the ones from your own Supabase project:
 *   Dashboard -> Project Settings -> API
 *     - projectId      = your Project Ref (the part before ".supabase.co")
 *     - publicAnonKey  = your "anon" / "public" API key (safe to expose in the browser)
 *
 * After updating these, rebuild the site (npm run build) and re-upload dist/.
 */

export const projectId = "YOUR_SUPABASE_PROJECT_ID"
export const publicAnonKey = "YOUR_SUPABASE_ANON_KEY"
