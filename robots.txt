# robots.txt for Farida Cajee-Botes Orthotist & Prosthetist
# https://www.cajeebotes.com

# Allow all search engines to crawl the site
User-agent: *
Allow: /

# Disallow admin panel from search engines
Disallow: /admin

# The patient intake form is NOT blocked here on purpose. It carries
# <meta name="robots" content="noindex, nofollow, noarchive"> on the page
# itself, and a crawler has to be allowed to fetch the page to read that tag.
# Blocking it here meant Google never read the noindex, so the address could
# still have been listed as a bare link if anyone linked to it. Keep the
# noindex tag on patient-intake/index.html; do not put the block back.

# Sitemap location
Sitemap: https://www.cajeebotes.com/sitemap.xml

# Crawl-delay (optional, helps with server load)
Crawl-delay: 1

# Specific instructions for Google
User-agent: Googlebot
Allow: /
Disallow: /admin

# Specific instructions for Bing
User-agent: Bingbot
Allow: /
Disallow: /admin

# Block common bad bots (optional)
User-agent: AhrefsBot
Crawl-delay: 10

User-agent: SemrushBot
Crawl-delay: 10
